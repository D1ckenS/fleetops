{
  "name": "marad-clone",
  "version": "0.0.0",
  "private": true,
  "description": "Maritime fleet management system. Working title — final product name TBD per CLAUDE.md §17.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24.15.0",
    "pnpm": ">=10.33.2"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "ci:full": "pnpm run lint && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
